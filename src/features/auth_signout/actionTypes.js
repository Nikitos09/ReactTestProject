// @flow
import { NAME } from './constants';

export const SIGNOUT = `${NAME}/SIGNOUT`;
export const SIGNOUT_SUCCEEDED = `${NAME}/SIGNOUT_SUCCEEDED`;
export const SIGNOUT_FAILED = `${NAME}/SIGNOUT_FAILED`;

export const REFRESH = `${NAME}/REFRESH`;
